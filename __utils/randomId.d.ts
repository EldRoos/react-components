/**
 * Generates random ID
 * @param {string} seed The seed to be injected at the front of the generated random ID
 * @returns {string} The generated random ID
 */
export declare function randomId(seed: string): string;
